int getSingle(int arr[]) {
        // using property 
        //x^0 = x         x^x = 0
       int res = 0;
        for(int i =0;i<arr.length;i++){
            res = res^arr[i];
        }
        return res;
    }
}
